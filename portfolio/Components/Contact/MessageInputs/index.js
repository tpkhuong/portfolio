import React from "react";
import InputsStyles from "./MessageInputs.module.css";

export default function MessageInputs({ children }) {
  return (
    <div className={MessageInputs[`inputs-sent-message-container`]}>
      {/* inputs */}
      {/* sent message */}
    </div>
  );
}
